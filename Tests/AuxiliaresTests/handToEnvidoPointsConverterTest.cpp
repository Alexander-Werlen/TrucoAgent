#include <gtest/gtest.h>
#include "../../Source/Auxiliares/handToEnvidoPointsConverter.hpp"

TEST(ExampleTest, primerEjemplo){
    EXPECT_TRUE(true);
    EXPECT_TRUE(isTrue(true));
    EXPECT_FALSE(isTrue(false));
}